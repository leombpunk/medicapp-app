# Sistema de gestión de pacientes y turnos - React Native

## Instalar proyecto

Recomiendo usar yarn sobre npm o pnpm ya que es más rapido y el unico compatible con expo

1. Instalar yarn

```bash
npm install --global yarn
```

2. Instalar el proyecto

```bash
yarn install
```

3. Iniciar el proyecto

```bash
yarn start
```

